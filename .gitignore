# PostgreSQL data volume — never commit
postgres_data/

# Go build artifacts
*.exe

# Environment secrets (template only)
.env

# Archivos GeoJSON de semillas (muy pesados para el repo)
politic-backend/scripts/data/
